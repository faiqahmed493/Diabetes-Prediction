# Diabetes-Prediction
This project implements a complete machine learning pipeline from scratch in C++ to predict diabetes using structured medical data. It includes both Decision Tree and Random Forest models with configurable parameters and evaluation metrics
